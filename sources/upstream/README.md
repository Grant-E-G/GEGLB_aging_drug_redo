# Upstream Sources

Use this directory only for temporary local upstream clones or archives. It is
ignored by git. Prefer recording immutable URLs, commits, checksums, and download
commands in `data_sources/dataset_manifest.md`.
