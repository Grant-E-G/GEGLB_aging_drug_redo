# Raw Data

This directory is ignored except for this README. Recreate local raw data with:

```bash
python scripts/download_data.py
```

Do not commit raw datasets unless a dataset-specific license and size review says
it is appropriate.
